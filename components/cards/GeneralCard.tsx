import { Box } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const GeneralCard = styled(Box)({
  maxW: "2xl",
  w: "full",
  boxShadow: "md",
  rounded: "md",
  textAlign: "left",
  mx: 2,
});
